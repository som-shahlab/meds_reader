#include <iostream>

// Simple smoke test executable.
//
// This file just prints a message to confirm the build and runtime
// environment are wired correctly.
int main() { std::cout << "Hello world, simple test" << std::endl; }
